@@@ expect exit=0
@@@ file CLAUDE.md
# Project

## Key Decisions

One line each; the full entry lives in the register.

### Area one

- **Context via `contextvars`** — short claim and its fence.
- **A shutdown shortens a *wait*; it never skips *work*** — fence.

## Out of Scope

nothing

@@@ file docs/decisions.md
# Key Decisions — full register

## Contents

- [Context via `contextvars`](#context-via-contextvars)
- [A shutdown shortens a *wait*; it never skips *work*](#a-shutdown-shortens-a-wait-it-never-skips-work)

---

## Area one

### Context via `contextvars`

Reasoning.

### A shutdown shortens a *wait*; it never skips *work*

Reasoning.
