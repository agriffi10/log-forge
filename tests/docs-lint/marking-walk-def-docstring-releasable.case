@@@ expect exit=1
@@@ match src/log_foundry/_lifecycle.py:7 claims the marking walk acts on EVERYTHING, with no scope.
@@@ match records everything inherited as
@@@ file src/log_foundry/_lifecycle.py
"""Lifecycle."""


def releasable(sink):
    """Whether this process may close a sink.

    **"Unrecorded is the caller's" is only sound because a fork makes it false first.**
    In a child, :func:`_mark_inherited` records everything inherited as ``_FOREIGN`` *before*
    any handler runs, so an unrecorded sink there is one built after the fork.
    """

@@@ file CLAUDE.md
# Project

## Key Decisions

Intro prose; every decision is a fence in its area file.

| Area | Fences |
|---|---|
| Area one | `docs/decisions/area-one.md#fences` |

## Out of Scope

nothing

@@@ file docs/decisions/INDEX.md
# Key Decisions — the register

## Areas

| Area | Fences | Governs |
|---|---|---|
| Area one | `docs/decisions/area-one.md#fences` | none |
@@@ file docs/decisions/area-one.md
# Area one — decisions

intro

## Contents

- [Fences](#fences)
- [Alpha rule](#alpha-rule)

## Fences

- **Alpha rule** — short claim and its fence.

---

### Alpha rule

Reasoning.
