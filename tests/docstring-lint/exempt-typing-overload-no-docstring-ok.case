@@@ expect exit=0
@@@ file src/log_foundry/example.py
"""A compliant fixture module."""

import typing


@typing.overload
def f(a: int) -> int: ...
@typing.overload
def f(a: str) -> str: ...


def f(a: object) -> object:
    """Does the thing.

    Args:
      a: The thing.

    Returns:
      The thing.

    Raises:
      None.
    """
    return a
