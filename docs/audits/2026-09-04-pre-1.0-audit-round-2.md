# Pre-1.0 Audit, Round 2 — 2026-09-04

> **Since this audit.** It was written against `main` at `456e9b7` and brought into the
> repository on top of `98c7e78`; line references throughout are against `456e9b7`. Between those
> two commits only PR #220 landed — `docs/invariants.md`, `tests/test_invariants_model.py`,
> `docs/process/`, `docs/templates/spec-template.md` and `CLAUDE.md` — which touches none of
> the files cited by line, so PR #220 moved no anchor, and §4's
> "architecture §12 open items are all still open" holds as written. **N1 and N4 are filed as open
> items in `architecture.md` §12**, against invariant 8, in the PR that brought this file in; each
> was re-run first, the N1 entry narrows the row — on SQLite the projected `function` column is
> the vector, and the same string as a *message* alone lands — and a third entry files what that
> measurement found beside it: `service`, `version` and `env` reach the event through no ceiling
> at all. §8 is the auditor's suggestion at
> `456e9b7`, not an assignment — none of its other items had landed at `98c7e78`. The body below
> is verbatim (hash-checked against its source), so a finding can be checked against its own
> evidence column.

Run against `main` at `456e9b7`, two days and thirty PRs after the 2026-09-02 audit at `561a9f6`
(SPEC-048, 050, 051, 052 landed; SPEC-049 and 053 are Draft). Five frames, all rotated from round
one: closure by re-running round one's own probes, an implementer's review of the fixes
themselves, the surfaces neither audit had touched (sanitize, ids, context, console echo,
sustained load), prose truth of everything written since, and the test suite's own gates.

**Outcome: the fixes are sound, the closure is real, and the remaining findings are new ground.**
33 of round one's 41 findings close on their original probes; two are partial and one is open,
five are deliberately deferred or recorded. The revision review built every SPEC-048 and SPEC-050
mechanism and found nothing that would change the built result. What round two found instead is
five defects in the surfaces that were never audited, and none of them is in the paths the last
three audits hardened.

Gates at `456e9b7`: `ruff`, `mypy --strict`, `spec-lint`, `docs-lint` (+corpus), `docstring-lint`
(+corpus 38/38) all pass; `pytest -n 12`: **2207 passed, 8 skipped, exit 0**. Tree clean throughout.
Probes under the session scratchpad `round2/{closure,revision,surfaces,prose,tests,mine}/`.

---

## 1. New findings (ranked)

**B** = fix before the tag; **S** = should fix before 1.0; **D** = record as a constraint; **c** = cosmetic.
Every runtime item was reproduced by running it; "read-only" is marked.

| # | Sev | Finding | Where | Evidence | Fix |
|---|---|---|---|---|---|
| N1 | **B** | **A lone surrogate survives `build_event` and kills the whole batch in every column-writing sink.** `_measured` encodes with `errors="replace"` to *measure*, never to *replace*, so a `surrogateescape` string (the exact case its docstring cites: `os.fsdecode(b"file-\xff.txt")`) leaves assembly intact. The JSON column is fine (`ensure_ascii`), but `SQLiteSink`, `PostgresSink`, `ClickHouseSink` bind `function`/`level` as raw `str` and the driver encodes strictly. Breaks SPEC-017's "safe by construction, not per sink" for three sinks, and via retry costs the innocent events sharing the batch. | `sanitize.py:96`; `sqlite.py:14`, `postgres.py:34`, `clickhouse.py:18` | `lf.info(bad)` + `@trace(name=bad)` on SQLite: `UnicodeEncodeError` absorbed, `lost 3 event(s); batch abandoned after 4 emit attempts`, `flush → abandoned`, `failed_batches=1`. Postgres/ClickHouse by reading (no extras). | make `truncate_str`/`text()` return the *replaced* decode, so no string leaves assembly that cannot be encoded |
| N2 | **B** | **`@trace` on a callable without `__qualname__` fails the caller at call time.** `fn.__qualname__` is read per call inside the wrapper, after decoration succeeded. | `decorator.py:707,743` | `lf.trace(functools.partial(f, 1))()` → `AttributeError: 'functools.partial' object has no attribute '__qualname__'`; same for a callable instance. Re-run by the parent. | resolve the name once in `decorate`: `name or getattr(fn, "__qualname__", None) or type(fn).__name__`; name a misordered `classmethod`/`staticmethod` at decoration |
| N3 | **S** | **`@trace` on a generator or async generator traces the *call*, not the iteration.** The span opens and closes before the body runs; every event logged inside the body is an orphan on a fresh trace. Nothing in README, architecture or the decorator mentions generators. Silently wrong is the worst of the three options. | `decorator.py:682` (only `iscoroutinefunction` dispatches) | sync: `['span.start','span.end']` before first `next()`; after exhaustion `('inside', parent_span_id=None)`. Async generator same. Re-run by the parent. | wrap generators (open on first `__next__`, close on exhaustion/`close()`), or refuse at decoration with a `TypeError` and document it |
| N4 | **S** | **A mapping *key* whose `__str__` raises replaces the whole sibling mapping, unmarked.** `key()`'s docstring says the int-key path exists so "one hostile key would not take every sibling with it, unmarked"; the non-int, non-float branch has that exact bug. A bad *value* is isolated correctly. | `sanitize.py:375` | `fields={"boom": {"sib": 1, KeyBoom(): 2}}` → `'boom': '<unserializable: dict>'`, `truncated=None` | wrap `:375` in the same guard `value()` uses; `<unserializable key: T>` and set `truncated` |
| N5 | **S** | **Echo to a broken pipe writes one stderr line per event, forever.** The caller is not killed (good), but the site does not use `_diag`'s throttled idiom. | `api.py:142-144` | `python p.py \| head -1` with 200k echoed events → `199,979` lines of `absorbed a failure while echoing to the console (BrokenPipeError)` | count and throttle like the queue-full site, or disarm echo after the first `BrokenPipeError`/closed-file `ValueError` |
| N6 | **S** | **README `in_span_lost` row still carries the clause SPEC-050 struck.** "Always the data, never the destination: the in-span path cannot fail at delivery" — the `Health` docstring strikes exactly that sentence and names a second cause (no drain thread); release-notes item 13 has it right, so README and notes disagree. | `README.md:1037` vs `worker.py:203` | `in_span_lost=4` with the data fine, worker `Thread.start` raising | reword the row to the two causes; the count tells them apart |
| N7 | **S** | **`pypi` environment has no protection rules and there is no tag ruleset** (round one D7, unchanged). Anyone with push can publish a stable release by pushing `v*`. | GitHub settings | `protection_rules: []`, `deployment_branch_policy: null`, `rulesets?targets=tag → []` | restrict the environment to `v*` tags or add a tag ruleset before the tag |
| N8 | S | **`get_config()` copies `defaults` one level deep while its docstring's reasoning implies the freeze is complete** ("sharing it would leave the freeze cosmetic at the one field that is not a scalar"). A nested mutable value mutated through the copy reaches every later event. SPEC-051's Out of Scope says "their own copy to do as they like with". | `config.py:235-240` | `probe_p`: nested `{'nested': 2}` → `99` visible to the library | deep-copy, or say "top-level copy; nested values are shared" in both places |
| N9 | S | **SPEC-049 (Draft) fences a state SPEC-051 already removed**, and five of its line anchors are stale. Out of Scope: "`**http_kwargs: object` on the seven platform sinks. `Unpack[TypedDict]` is additive" — the signature is already `Unpack[HTTPAuthKwargs]`. Stale: `http.py:844` (`:976`), `elasticsearch.py:88` (`:92`), `file.py:358` (`:371`), and the three "SPEC-052 sweep" refs. Its premises otherwise all hold on `main` (probed: `ClickHouseSink(chunk_size=-5)` emit returns normally with 0 inserts; `interval=0` leaves 2 of 15 lines; every construction it names is accepted). | `docs/specs/SPEC-049-*.md` | read-only + `probe_spec049.py` | drop the `Unpack` fence; anchor to symbols not lines |
| N10 | D | **Nested-span memory is O(depth²)** — the span stack is a copied tuple and each `Token` pins its predecessor. Irrelevant at real depths (100 deep ≈ 50 KB), stack clean afterwards, but §13 does not say so. | `context.py:94` | tracemalloc peak: depth 1000 → 5.3 MB, 2000 → 18.6, 4000 → 69.2, 8000 → 266.5; 10k nested → RSS 483 MB | one line in architecture §13 |
| N11 | D | **One event can legally be ~2 MB** (`max_keys` × `max_value_bytes`), serializable and marked; downstream limits are the sink's problem via `dropped_oversized`. Already implied by "ceilings bound per value"; measured so the number exists. | `config.py:44-47` | `event size bytes 2060506`, `truncated=True` | note the arithmetic in §6 |
| N12 | c | Kinesis `_partition_key` docstring and SPEC-048 FR-003 say the service limit is "256 **bytes**, never 256 characters"; the API reference says 256 characters. The byte bound is safe (stricter), so delivery is unaffected; only a non-ASCII key between 256 bytes and 256 characters lands on a different shard. The per-record charge itself is correct per the same reference. | `kinesis.py:289` | read-only | reword: "bounded to 256 bytes, at or below the service's 256-character limit" |
| N13 | c | Release notes "items 13 to 15 are the quietest … and are last" — there are 16 items. HANDOFF banners say #126/#127 "both still open as of 2026-09-02" — both closed. `component-inventory.md` names no SPEC-048 helper (`_NoRedirect`, `_drain_pending`, `_partition_key`). R11's stderr line still says "lost 1 item(s)" for a swap that lost nothing (recorded in SPEC-050 Out of Scope). `Health.dropped` counts *submissions* (documented) so the diag line under-reads events ~3× under overload. `parse_traceparent` accepts surrounding whitespace, undocumented. | | | |
| N14 | c | `RELEASES.md` has no `v1.0.0` row by design ("replaced the moment the tag is cut") — that edit must land in the tagged commit beside the notes file or the table stays a release behind. A tagging-checklist line, not a defect. | `docs/spec-delivery/RELEASES.md:10-13` | | |

## 2. Round one closure (original probes, re-run as written)

**Closed 33 · partial 2 · open 1 · deferred/documented 5.** Full 41-row table with decisive
output lines in `round2/closure/`. The shape:

- **R1 redirect** — closed: 302 → `SinkDeliveryError`, `losses=(0,1)`, the landing origin saw no request; one opener for the family, Sentry's fallback inherits it. 300/301/302/303/307/308 each count once.
- **R2 AWS partial failure** — closed: chunk 2 of 3 raising → 15 landed, `failed=10` once, no raise; all-fail → raises `failed=25`; single-chunk raise → raises so the worker retries; `KeyboardInterrupt` propagates. The guard is inside `_send`. (Round one's own probe now reads `duplicates=15` because its "retry" is an unconditional second `emit()` — a probe artifact anyone re-running it should know about.)
- **R3 / R4 / R5 / R6 / R9 / R11 / R13 / R14** — closed on their probes. R4's wait is `min(DEFAULT_CLOSER_GRACE, remaining)` on both paths, a recorded decision rather than the audit's "remaining deadline": a 3 s close-is-delivery sink is still killed at exit.
- **R7 / R8 / R10** — deferred to SPEC-049, untouched as SPEC-048's Out of Scope says. **R15** partial (gzip and rotation duplicate closed; socket attempt count, Logstash `url`+`host`, Sentry DSN, `app_name` whitespace → SPEC-049).
- **R12** documented constraint (`docs/decisions/`). **P6** documented: the tunables stay unreachable by decision; `DEFAULT_SWAP_TIMEOUT` is now exported.
- **P1..P5, P7..P9** closed (kw_only → `TypeError` on positional; `context.__all__` is six names; `Mapping` accepted; docstrings corrected; `Unpack` typed; names exported; naming recorded as frozen).
- **D1..D6, D8..D10** closed; **D7** open (N7). **C1** closed by decision (cap re-scoped to the summary line, gated, corpus green); **C2..C5, C7** closed; **C6** partial (→ SPEC-049 FR-007; round one's "postgres lacks the trio" was its own checker tripping on a dedented paragraph — the trio is present).

## 3. The revision (SPEC-048 / 050 / 051 / 052), reviewed as an implementer

**Sound.** Built against, with probe paths in `round2/revision/`: redirect refusal for every 3xx
and every subclass, with the injected `opener=` honoured verbatim; per-chunk guards in all four AWS
sinks across partial, total, single-chunk, retry-after-partial and `KeyboardInterrupt`; Pub/Sub
bounded close with no double count when a future settles after the deadline; taken markers
answered `abandoned` on the timeout path and `ok` when delivered before it; the in-flight close
`Event` (second caller waits ≤ grace, third waits 0, a raising close leaves no permanent wait,
`shutdown(0)` bystander waits 0), orphan-path twin identical; `in_span_lost` counts by event
(5 sync, 4 async) without double-counting `orphan_lost`; the post-put `_shutdown_done` re-read;
kw_only/`Mapping` with the whole repo swept for positional construction and every `defaults`
copied. **A third unbounded flush wait was hunted and not found**: every park on the flush path
(`_put_marker` slice loop, `marker.event.wait`, `swap_sink` on `flush(None)`, the two close
waits, the sink `flush` hooks) is released by something; what remains requires
`shutdown(timeout=None)` or no shutdown at all, the caller's stated choice. Twins checked are
listed in the agent's report.

## 4. Prose truth

Every bullet of `docs/release-notes/v1.0.0.md` and all 16 upgrade items were checked against
`main` and, for the "old behaviour" halves, against `git show v0.10.1:` — **all true** (table in
`round2/prose/`), except the "13 to 15" count (N13). `release.yml` reads the notes file at the
tag ref. The six new and four extended `docs/decisions/` entries agree with their fences and
with code. Architecture §12 open items are all still open in code; nothing fixed-but-listed.
SPEC-053's premise reproduced with its own pipeline (6/10 at 23fe6cc, 0/9 at ac938c0).
Delivery docs for 048/050/051/052 name only symbols that exist. `INDEX.md` status matches all 53
headers. README sink signatures: 36 rows, zero mismatches. `main` green on every workflow at
456e9b7; 0 open PRs, 0 Dependabot/CodeQL alerts.

## 5. The test suite's own gates

Measured in a scratch copy with `log_foundry.__file__` verified to resolve to the copy:
- **An `ImportError` inside `worker.py` now errors collection** ("Interrupted: 1 error during collection") rather than skipping — SPEC-052 FR-004..006 hold. `importorskip` survivors: 3, all guarding the real `nats` extra.
- **`tests/test_typed_consumer.py`** reads the planted errors out of `rejects.py` itself, requires ≥13, asserts every one is reported and the reported count equals the planted count, and proves `MYPYPATH` is consulted by pointing it at a stale tree. Sound.
- **Vacuous-test sweep of the ~8,700 new test lines** (reading, by a sub-reviewer): five "worst" candidates. I mutation-tested the top two and **neither survives**: deleting `_take_marker` from `_final_drain` fails exactly the test the sweep said would stay green; deleting the clean-path `_release_waiters()` is caught by a sibling (`test_a_flush_marker_queued_after_the_final_drain_is_still_answered`), so the named test alone does not pin it but the suite does. The remaining three candidates and eleven lesser ones are unverified reading and are listed in `round2/tests/` for the maintainer; on this evidence they are "a sibling pins it" at worst, not gaps.
- **Not done this round:** mutating `scripts/docstring-lint.py`'s own checks against its corpus (the corpus passes 38/38 and CLAUDE.md records that it mutation-tests every exemption; not independently re-proved here), and the eight stash-the-fix runs (superseded by §3, which exercised every guard live).

## 6. Verified clean (so the next audit skips it)

37 adversarial values through `build_event` on all three event kinds (cycles, raising
`__iter__`/`__str__`/`encode`, lying `__len__`, invalid UTF-8 bytes, 10 MB strings, 100k keys,
1000-deep nesting, `Decimal`/`datetime`/`Enum`/dataclass/set/tuple, `-0.0`, `1e400`, NaN,
10^100000 ints, non-str keys, NFC/NFD): caller never fails, every event `allow_nan=False`-clean,
every ceiling held exactly, `truncated` exact except N4, reserved keys never overwritten from any
of the four routes. `traceparent`: 21 malformed forms handled per W3C; `baggage` limits exact at
8192/8193 bytes; round-trips byte-for-byte; 10^6 span ids unique from `os.urandom`. Context
isolation across `gather` ×100 nested, `to_thread`, `copy_context().run`; pool threads correctly
see no span. `StopIteration`/`GeneratorExit`/`SystemExit`/`KeyboardInterrupt` re-raised and
recorded. Echo: one `write()` per line, raising/closed stream absorbed, 4 threads × 5000 lines
uncorrupted. Load: 200k in-span `info()` at 5 µs/call with RSS returning after flush; 100k root
spans flat at 29 MB; sustained overload never blocks the caller (max `info()` 172 µs), `queued`
pins at 10,000, `delivered + dropped == submitted`; 1,000 threads × 100 spans reconcile; 1,000
`configure()` swaps leak no threads or closers; 10k idle `flush()` calls at 11 µs with no marker
growth. `FileSink`/`RotatingFileSink` under six concurrent appending processes: 0 of 72,000
lines torn, because `O_APPEND` plus whole-`write()` flush boundaries never split a line;
`SQLiteSink` 4 procs × 6000 rows, 0 lost.

## 7. Not covered

Postgres/ClickHouse/Mongo halves of N1 (read only; same column shape). Real boto3/google/pika
clients. `RotatingFileSink` rotation racing another process's append. Free-threaded builds,
Windows. The docstring-lint mutation and the stash-the-fix runs (§5). Scorecard posture alerts.
Each auditor was capped; what fell below its cut is not here.

## 8. What I would do before the tag

1. N1, N2 as one small spec (assembly safety and the decorator's name resolution): both fail
   the caller or lose a batch on ordinary input, in paths no audit had exercised.
2. N3, N4, N5 beside it or in SPEC-049's neighbourhood; N3 needs a product decision
   (wrap vs refuse) before it is specced.
3. N6, N8, N9, N12, N13 are a docs PR; N7 is a settings change; N10, N11 are two lines in
   `architecture.md`; N14 goes on the tagging checklist with the `RELEASES.md` row.
4. SPEC-049 as already planned. SPEC-053 is a gate for a class that has now recurred three
   times and is worth landing before the tag rather than after.
