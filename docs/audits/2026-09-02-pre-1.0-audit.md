# Pre-1.0 Audit — 2026-09-02

> **Re-run by `2026-09-04-pre-1.0-audit-round-2.md`**, which re-ran round one's probes as written
> against `456e9b7`: 33 of the 41 findings close, two are partial (R15, C6), one is open (D7,
> carried forward there as N7) and five are deferred or recorded. Read round two for what stood
> at `456e9b7`; read this one for the evidence behind SPEC-048, 050, 051 and 052.

Third audit of this project, run against `main` at `561a9f6` (`v0.10.1-92-g561a9f6`). Five
surfaces in parallel: the public API as it will freeze, code-standards conformance, silent data
loss and lifecycle, the sink family, and docs / packaging / release. The 2026-08-07 audit held the
tag; this one re-ran every finding it recorded and then hunted fresh.

**Outcome: the runtime is close, the release surface is not.** Every A/L/S/C finding of the
previous audit is closed except two residues (below). No finding contradicts a README promise the
way A1–A3 did. But two sink defects lose or duplicate events with every counter at zero, the README
still carries four of its seven recorded defects, there are no 1.0 release notes on `main`, and the
published metadata links to nothing. Nothing here is a reason to hold the tag for long; all of it is
a reason not to cut it today.

Every runtime finding was **reproduced by running it** unless marked read-only. Probe scripts are
under the session scratchpad (`api/`, `standards/`, `lifecycle/`, `sinks/`, `release/`).

Gates on this commit: `ruff`, `mypy --strict`, `spec-lint` (0 failed, 5 size warnings), `docs-lint`,
`docs-lint-test` (44/44) all pass; `pytest` serial: **1915 passed, 8 skipped, exit 0**. `main` is
green on every workflow. Working tree untouched throughout (`git status` clean).

---

## 1. Runtime defects (ranked)

Severity is *for 1.0*: **B** = fix before the tag (loss or duplication invisible to `health()`, or
a contract broken with a bound); **S** = should fix before 1.0; **L** = low; **D** = already a
documented constraint, listed so it is a decision rather than an omission.

| # | Sev | Finding | Where | Evidence | Fix |
|---|---|---|---|---|---|
| R1 | **B** | **`HTTPSink` loses a batch on 301/302/303 and forwards the bearer token to the redirect target.** The default `urlopen` opener follows redirects, rewriting the POST into a body-less GET and keeping `Authorization`. An `http://` collector behind a load balancer that 301s to `https://` loses everything, every batch, reported as delivered. Reaches every HTTP-family sink and Sentry's HTTP fallback. | `sinks/http.py:277,867` | probe: server 302s to a second origin → `[('POST','/post','Bearer secret-token',685), ('GET','/landing','Bearer secret-token',0)]`, emit returned, `losses=(0,0)` | private opener with a `HTTPRedirectHandler` whose `redirect_request` returns `None`, so a 3xx becomes `HTTPError` → `_abandon("HTTP 302")` (counted, announced, raised) |
| R2 | **B** | **The four AWS batch sinks raise on *partial* failure when the client raises mid-batch, so the worker's retry re-sends the chunks that landed.** The client call is unguarded; a `ClientError`/`EndpointConnectionError` on chunk N propagates after chunks 1..N-1 delivered. The exit drain (5,980 events = 598 SQS chunks) is exactly this shape. `base.py` says "do not raise on partial failure". | `sinks/sqs.py:361`, `sns.py:169`, `kinesis.py:219`, `firehose.py:221` | probe: 25 events / 3 chunks, client raises on call 2 → first emit raised, retry returned, `delivered=35 duplicates=10 losses=(0,0)`; Kinesis 600 records → `duplicates=500` | guard per chunk as `clickhouse.py:199-214` does: count the chunk into `failed`, announce by type, raise only when nothing delivered |
| R3 | **S** | **`flush(timeout=None)` parked behind a stuck sink is never released when `shutdown(timeout)` expires.** The timeout branch returns before `_release_waiters()`. The `_release_waiters` docstring says it exists for exactly this caller. | `worker.py:951-964` | probe: `flusher still waiting after shutdown gave up: True` | call `_release_waiters()` in the timeout branch; markers keep `delivered=False` |
| R4 | **S** | **`shutdown()` first called on a daemon thread: the `atexit` call returns while the inline `close()` is still running, and exit kills it.** `_close_if_owed` finds the close already claimed and returns; the orphan path has the same shape. C3's residue. | `worker.py:936-943,1053`; `_lifecycle.py:1529-1537` | probe with a close-is-delivery sink: `main exiting at 0.31s; closes started=1 wire=0 buffered=9`, no `_diag` line | record an in-flight inline close (an `Event`) and have the idempotent path wait on it with the remaining deadline, as it already does on `_drain_settled` |
| R5 | **S** | **`GooglePubSubSink.close()` is unbounded while its `flush()` is bounded.** `_resolve(future, timeout=None)` per pending future; the client's publish deadline is 600 s and `Worker.shutdown` closes inline. K6's shape, unfixed here. | `sinks/pubsub.py:516-517` | probe: `flush()` raised at 0.50 s; `close()` with the stop signal set returned in 3.50 s (would be the client deadline against a dead destination) | run `flush()`'s deadline loop, count the remainder as `failed` and announce (the `KafkaSink._flush_bounded` rule) |
| R6 | **S** | **A worker that cannot be built (`Thread.start` raises) loses every span's events with a stderr line and no `Health` term.** `_reinit_after_fork` sets `stopped_reason` for the identical failure; this path counts nothing. | `decorator.py:566-568` → `_lifecycle.py:1197` | probe: stderr `absorbed a failure while closing a span (RuntimeError)` ×2, then `Health(stopped_reason=None, in_span_lost=0)`, delivered 0 | increment `in_span_lost` by event count in `_end`'s except, or surface a reason |
| R7 | L | **Bad sink configuration surfaces at first emit as a raw non-`SinkDeliveryError`, uncounted, forever.** Only `failed_batches` moves. | `http.py:261-266` (`timeout=-1/nan/inf`, CRLF in header name or value, non-http URL), `_socket.py:161`, `clickhouse.py:88,145`, `postgres.py:104` (`chunk_size=0` → 4 attempts + backoff per batch), `pubsub.py:135` (`overflow_timeout=nan` never expires) | probes per site | `_require_positive` / `usable_timeout` at construction; reject CR/LF in headers; parse the URL and require `http(s)` |
| R8 | L | **`RotatingFileSink` accepts `interval<=0`, `max_bytes<0`, `backup_count<0`**; `interval=0` rotates on every emit and the ring destroys batches. | `sinks/file.py:266,380` | probe: 3 emits × 5 events → 10 remain | `ValueError` at construction |
| R9 | L | **`SentrySink.close()` does not flush the SDK transport**; `flush()` does, `close()` forwards only to the HTTP fallback. `shutdown()` alone strands captured events in the SDK's worker (the frozen-Lambda case). | `sinks/sentry.py:248-252,273-274` | probe: 25 captured, `flush() calls after close()=0` | call `self.flush()` (absorbing) before the forward |
| R10 | L | **Driver-default waits left unbounded** where SPEC-041/047 bounded Postgres and NATS: pymongo `socketTimeoutMS=None`, pika `blocked_connection_timeout=None` (blocks `basic_publish` under the emit lock), ClickHouse `send_receive_timeout` 300 s × 4 attempts. read-only. | `mongodb.py:74`, `rabbitmq.py:254`, `clickhouse.py:99` | | expose with bounded defaults, or record each in architecture §13 as `DEFAULT_CONNECT_TIMEOUT` is |
| R11 | D | **An incomplete swap leaves the previous sink open forever; a client-buffering sink's buffer dies with the process.** Documented (README:1009, arch:328). Worth reconsidering: once `shutdown()` has joined the drain thread the old sink is unused and a close is safe and owed. | `worker.py:765-767` | probe: `incomplete_swaps=1`, then after shutdown `A.closes=0 A.buf(unflushed)=3` — the stderr line counts 1, not 3 | |
| R12 | cosmetic | `health().retired` reads `True` for a worker built *after* an orphan-only `shutdown()` while it is live and delivering | `_lifecycle.py:2022` | probe: `retired=True`, `flush()` ok, events delivered | |
| R13 | cosmetic | `Worker.submit`'s unlocked `_shutdown_done` read can queue one uncounted event after the final drain; never lost (`delivered+queued==spans` over 5 fuzz trials), possibly unannounced | `worker.py:448-451` | | |
| R14 | cosmetic | Kinesis per-record limit ignores the partition key (K9 residual); Kinesis rejects the whole `PutRecords`, which via R2 duplicates earlier chunks | `kinesis.py:183` | probe: `data=1048576 + key=200` passes | charge the key per record as the request budget already does |
| R15 | cosmetic | `SocketTransport` abandonment line prints `max_retries+1` attempts on the permanent-errno path after one send (HTTPSink already fixed the same) · `gzip=True` overwrites a caller's `Content-Encoding` (`http.py:819`) · `LogstashSink` silently ignores `host`/`port`/`transport` when `url` is given (`logstash.py:127`; contradicts the SPEC-043 rule) · `LoggingSink` cannot report a handler that absorbs its own failures (`logging_sink.py:87` docstring overstates) · `RotatingFileSink` rename failure mid-batch duplicates pre-rotation events (`file.py:444`) · `SentrySink` accepts an unparseable DSN (`sentry.py:571`) · whitespace in `SyslogSink app_name` / `ElasticsearchSink index` corrupts the frame | | | |

**Bound claims hold** (CPU 0.000 s on every probe): `flush(1)` against an emit blocked forever →
1.00 s `timed-out` (also `queue-full` at 1.00 s); `shutdown(1)` → 1.01 s, `ShutdownTimeout`,
second call 0.00 s. `close()` is unbounded on both paths, as architecture §13 states (3.00 s against
`shutdown(1)`) — R4 and R5 are the two places that matters.

## 2. The API as it will freeze

Nothing blocking. The surface is 28 top-level names, 35 sink classes in 31 modules, every `__all__`
correct, `py.typed` ships, and a `mypy --strict` consumer exercising every top-level name plus 16
sinks with every documented kwarg is clean. Cheap now, breaking later:

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| P1 | S | **Public dataclasses accept positional construction**, freezing field order for third-party constructors (`Health` has 12 fields). Nothing in `src/` constructs them positionally. | `worker.py:61`, `config.py:15`, `sinks/base.py:23`, `results.py:10` | `@dataclass(frozen=True, kw_only=True)` |
| P2 | S | **`context.__all__` exports `current_span`** (and `push_span`/`pop_span`/`push_baggage_scope`/`pop_baggage_scope`) while the same module documents it as "internal and hands back a mutable `Span`". Removing after 1.0 is breaking. | `context.py:16` vs `:323` | trim `__all__` to the six names `log_foundry` re-exports |
| P3 | S | **`configure(defaults=)` and `trace(defaults=)` are `dict[str, object]`** (invariant): a typed consumer cannot pass `dict[str, str]`. The exact defect SPEC-034 FR-004 fixed on `fields=`. | `config.py:95`, `decorator.py:611,619` | `Mapping[str, object] \| None` |
| P4 | S | **`Health` docstring claims "index access to every earlier field stays as it was"** — false since it became a dataclass (`len(health())` → `TypeError`). The README says the opposite, correctly. | `worker.py:64-68` | drop "and index" |
| P5 | S | **`health()` docstring lists 9 of 12 fields**: `inherited_sink` is never mentioned, `orphan_lost`/`in_span_lost` absent from Returns. | `__init__.py:96-100` | add the three |
| P6 | S | **README names `batch_size`/`flush_interval` as the remedy for `health().dropped`; no public API reaches them** (D4/P8, open by SPEC-034's deliberate deferral). `DEFAULT_SWAP_TIMEOUT` still unexported. | `README.md:1003`, `_lifecycle.py:1197` | reword the row, or ship the additive `configure()` knobs |
| P7 | cosmetic | Seven sinks take `**http_kwargs: object` (P9 residual): `timeout="not-a-float"` passes mypy and construction | `elasticsearch.py:51` and six siblings | `Unpack[TypedDict]`, additive in 1.x |
| P8 | cosmetic | Names used in public signatures not exported: `GroupIdSource`/`DedupIdSource` (`sqs.py:29`), `Backend` (`sentry.py:17`); `flush_sink` in `sinks.base.__all__` but not top-level, unlike `read_losses` | | add to `__all__` |
| P9 | cosmetic | Deliberate per SPEC-034 Out of Scope, recorded so the next audit does not re-find them: `PackageNotFoundError` un-aliased in the top-level namespace; `flush()`'s `5.0` literal vs `shutdown()`'s exported constant; `PostgresSink.connect_timeout: int`; the family's naming drift — pre-built client is `client`/`producer`/`connection`/`opener`, connection string is `url`/`dsn`/`uri`/`servers`/`bootstrap_servers`/`connection_str`, routing key is `key_field`/`partition_key_field`/`message_group_id`, batch size is `max_batch_count`/`chunk_size`/`max_pending`, timeouts are `timeout`/`flush_timeout`/`publish_timeout`/`connect_timeout`/`overflow_timeout`, destination positional in most and keyword-only in four; `create_table` defaults True for SQLite and False for Postgres/ClickHouse | | a one-line "the naming is frozen as is" note in the decisions register, or one normalising spec now |

Console echo (P7 of the prior audit) remains reachable only via `api._console`; deferred as additive.

## 3. Docs, packaging and release readiness

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| D1 | S | **README carries four of the seven recorded defects; the fix PR (#126) is a draft that now conflicts with `main`.** D1 `error` row `{type, stack}` vs emitted `{type, module, message, stack}` (`:1236`); D4 `batch_size`/`flush_interval` remedy (`:1003`); D5 "never blocks on sink I/O" unqualified at `:17-18`, `:142`, `:976` while `:541` concedes the orphan path emits synchronously; D7 module list omits `sanitize`, `_diag`, `_lifecycle`, `results`, `_fork` (`:1283`) and `duration_ms` samples are hand-rounded. Also `:46` `SQSSink(url, ...)` — the parameter is `queue_url`; `:558-559` `stream=sys.stdout` — real default `None`; CI table omits `docs-lint.yml`; no link to release notes or `RELEASES.md`. | `README.md` | close #126, re-apply against current `main` |
| D2 | S | **No 1.0 release notes on `main`.** `docs/release-notes/` does not exist; PR #127 (draft, mergeable, 2026-08-07) holds notes covering SPEC-024..033 only and the `release.yml` change to read them; as is, the job runs `--generate-notes` over 92 commits. `docs/spec-delivery/RELEASES.md` ends at `v0.10.1 → SPEC-023`; SPEC-024..047 have no row. | `release.yml:629-635` | refresh #127 to SPEC-047, merge the workflow half before tagging, add the `v1.0.0` row |
| D3 | S | **Published metadata has no project URLs, no `Development Status`, no keywords.** PyPI page links to nothing. Classifiers are Poetry-derived and include `3.14`, which is not CI-gated. | `pyproject.toml` | `[project.urls]` (Repository, Releases, Security), `Development Status :: 5`, `Typing :: Typed`, `Topic :: System :: Logging`, keywords |
| D4 | S | **`SECURITY.md` supports "Latest `0.x` release"** — stale the moment the tag exists. | `SECURITY.md:11` | "Latest release" |
| D5 | S | **Three Dependabot PRs open 3–4 weeks** (#155 zizmor-action, #164 pip group, #165 codeql upload-sarif). CLAUDE.md's fence is "pins are maintained, not frozen". | | merge or close before tagging |
| D6 | L | **`release.yml` installs `build` and `twine` unpinned in the job that produces the published artifact** (Scorecard `PinnedDependenciesID` open); same for `pipx install poetry` in three workflows. | `release.yml:38`, `ci.yml:98`, `integration.yml:78`, `pip-audit.yml:57` | pin versions |
| D7 | L | **`pypi` environment has no protection rules and no tag ruleset**: anyone with push can publish a stable release by pushing a `v*` tag. Single maintainer, so low. | GitHub settings | restrict the environment to `v*` tags, or a tag ruleset |
| D8 | cosmetic | **Stale session handoffs**: `docs/audits/HANDOFF-2026-08-07.md` announces "two regressions on `main` right now", `HANDOFF-2026-08-10.md` says "next spec is SPEC-038"; only `HANDOFF-SPEC-039-phase-3.md` carries a superseded banner. `docs/process/completion-ritual.md` says `docs/audits/` is history. | | delete or banner |
| D9 | cosmetic | `docs/implementation-guide.md:42-45` presents `core.py` (`LogForge`) and `modules/v1/log.py` as "your current" scaffolding; CLAUDE.md records both removed | | delete the paragraph |
| D10 | cosmetic | `docs/component-inventory.md` `health()` row lists 3 of 12 fields; `.gitignore` does not cover `sbom.cdx.json`, which CLAUDE.md's documented command writes at the root; zizmor `self-repository` low on `release.yml:14`; `dependabot.yml` comment still describes the extras-floor rewrite as a failure mode while `v0.9.0` accepted the floor it produced | | |

## 4. Standards misalignment

The gates check the mechanics; these are the rules the gates do not check, swept over the whole
population, not sampled.

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| C1 | S | **The "≤3 sentences" docstring rule is dead in practice: 179 of 508 defs (35%) exceed it, up to 45 sentences.** `_lifecycle.py` 30, `worker.py` 18, `decorator.py` 9, `_fork.py` 8. The cause is the rule's own neighbour — "reasoning that would have been an inline comment belongs in the docstring" — so the cap contradicts the practice it sits beside, and no gate checks any part of the docstring rule. `docs/process/completion-ritual.md`: a rule practice consistently violates gets reconciled or deleted. | `CLAUDE.md` Code Conventions; worst `_lifecycle.py:1641` (32), `:1454` (23) | re-scope the cap to the summary line or delete it; gate the parts kept (no inline comments — currently 0 violations; sections present — 1 miss; one-line module docstrings — the two asserted exceptions) with the checker at `standards/check_docstrings.py` |
| C2 | S | **The Python rulebook contradicts CLAUDE.md and `pyproject.toml` on three settled points**: runtime "≥ 3.13" (`python.md:3,12`; floor is 3.12), "omit `Returns:` for `None`" (`:165`; CLAUDE.md requires `None.`), and how to write inline comments (`:168`; CLAUDE.md forbids them). The INDEX routes every Python task there first. | `docs/best-practices/python/python.md` | correct the floor; point the two docstring bullets at CLAUDE.md |
| C3 | S | **The test suite still carries its pre-implementation scaffolding: 175 `importorskip`/`skipif` guards on modules that shipped long ago**, in 19 files at module level, several with reason "not implemented yet". An `ImportError` inside `worker`/`_lifecycle`/`config` now skips whole files silently instead of failing; `test_worker_predicate_roster.py:51` already says why that is wrong. `tests/README.md` still says the tests are "written ahead of the implementation". | `tests/conftest.py:1-8,194-200`, `test_decorator_sync.py:15-20`, `test_worker.py:18-21`, `test_config.py:122-125` | plain imports, drop the marks and the `lf` fixture's `hasattr` skip, rewrite `tests/README.md` |
| C4 | cosmetic | Dated measurements in always-loaded docs (`CLAUDE.md:80` "~35 s not ~133 s", `:111` "308 lines", `:230` "89,340 bytes"; `tests/README.md:20`) — the global rule says dating does not save them | | state the principle, drop the numbers |
| C5 | cosmetic | CLAUDE.md Layout names three "leaf helpers no module map anticipated" but not `_fork` or `_lifecycle` (the two largest additions) | `CLAUDE.md` Layout | one clause |
| C6 | cosmetic | Residual literal violations: `sinks/postgres.py:238` `_reconnect_if_broken` lacks Args/Returns/Raises (the only miss in 449 functions); `sinks/__init__.py` has no module docstring; `file.py:357` `@staticmethod` (python.md §9 says never); measured counts in `src/` docstrings anchored to a spec not a commit (`nats.py:23`, `http.py:163`, `pubsub.py:89`, `loki.py:93`); two tight wall-clock budgets (`test_worker.py:116` `< 0.1`, `test_sinks_pubsub.py:404` `< 0.2`) | | |
| C7 | env | **Local venv lacks `pytest-xdist`** (added in #190), so `poetry run pytest` fails with "unrecognized arguments: -n"; the lock has it. Not a repo defect. | `.venv` | `poetry install --with dev`, then `git checkout -- pyproject.toml` |

## 5. Prior findings (2026-08-07) — closure

| Item | Status | Evidence |
|---|---|---|
| A1/L1 `flush()` blind to an open span | fixed | `flush()` inside a span → ok, `['span.start','inside']` delivered |
| A2 `info(exc)` fails the caller in-span | fixed | returns normally, stderr names `AttributeError`, `in_span_lost=1` |
| A3/P1 `get_config()` live singleton | fixed | frozen copy; mutation → `FrozenInstanceError` |
| L2 / L2b / C4 fork | fixed | child `flush=ok`; 8/8 children exit 0 with the parent's drain thread inside `FileSink._lock` |
| L3 `flush()` cannot reach a client buffer | fixed | sink `flush` hook called: `wire 4, client buffer 0` |
| L4 asyncio task appends to an emitted span | fixed | late event delivered on a fresh trace |
| L5 dead `MultiSink` child invisible | fixed | `health.sink=SinkLosses(0, 3)` |
| L6 orphan loss invisible | fixed | `orphan_lost` counts, stderr lines |
| S1 NaN/Infinity | fixed | `'<float: nan>'`, `truncated=True`, strict JSON parses |
| C1 orphan steals the stop signal | fixed | signal stays the worker's through an orphan storm |
| C2 `configure(B)` racing `shutdown()` | fixed | 30/30 trials both closed, config is B |
| C3 `atexit` abandons an in-flight `shutdown()` | fixed for the drain, **open for the close** → R4 |
| C5 stderr under the lifecycle lock | documented constraint (arch §13); exactly the three documented sites, no others |
| K1 exit drain not re-chunked (HTTP) | fixed | 5,980 events → 52 requests, 3×413, `losses=(0,0)` |
| K2 / K3 Postgres rollback / reconnect | fixed | probed |
| K4 Pub/Sub retains futures | fixed (read-only); residual R5 |
| K5 Firehose newline | fixed |
| K6 Kafka close unbounded | fixed | `close()` → `flush(0.2)`, 7 counted into `failed` |
| K7 syslog EMSGSIZE retried | fixed; diag miscount → R15 |
| K8 Redis unbounded | fixed (opt-in `maxlen`) |
| K9 Kinesis/Firehose margins | mostly; per-record key → R14 |
| K10 Logstash content type | fixed |
| P2 reserved words | fixed | all five keys land in `fields` |
| P3 exports | fixed |
| P4 `stop_signal` | fixed | `log_foundry_stop_signal`; foreign attribute untouched |
| P5 one-bit `flush()` | fixed | `FlushResult`/`ContinueResult` |
| P6 `NamedTuple`s | fixed; docstring residue → P4 above |
| P7 console echo | open, deferred as additive |
| P8 worker tunables | open, deferred as additive → P6 above |
| P9 naming | `sinks.util` gone; rest recorded → P7/P9 above |
| D1..D7 README | **D1, D4, D5, D6 open; D7 partial; D2, D3 fixed** → D1/D2 above |

## 6. Verified clean

So a later reader does not re-audit them. Lock order (`_sweep_lock → _state._lock → _config_lock →
_owned_lock`; `_state._lock → {_closing_now_lock, _closers_lock}`; `Worker._lock` a leaf) with no
inversion and no sink call under a process-wide lock; every `Health` counter read under the lock
that writes it; a 40-seed 4-thread fuzz over `{trace, info, flush, configure(new), shutdown}`
holding `delivered + queued == spans` and every sink that took an event closed; async cancellation
inside `@trace` recorded and re-raised; `flush()` from inside a running loop; two tasks sharing a
span; fork during an in-flight swap and with a marker queued behind a slow emit; `ThreadPoolExecutor`
jobs at interpreter exit; `atexit` ordering against `logging.shutdown`. Zero inline comments in
`src/`, all 31 `type: ignore` and 4 `noqa` carry codes; no module-level third-party import; every
extra has a consumer; no import cycles; no `print` outside `_diag`/`stdout`; no TODOs; 24
assertion-less tests are all "must not raise". Every README import resolves and every documented
sink signature matches `inspect.signature` (37 rows); all 24 action pins are `@<sha> # vX.Y.Z`;
least-privilege permissions; OIDC only, no repository secrets; the wheel from a `v1.0.0` tag is
`log_foundry-1.0.0-py3-none-any.whl` with `py.typed` and `LICENSE`, no tests/docs, `twine check`
passes; `poetry check --lock` clean; `pip-audit --strict` over every extra clean; SBOM builds (43
components); Dependabot and CodeQL alerts 0. HTTP family: 500 retried then counted, 400 abandoned
after one attempt, hanging server bounded, `Retry-After` clamped, stop signal cuts backoff to 0,
oversized event dropped without a request, no URL/token/header/body text on stderr. Socket family:
refused port bounded at 0.52 s, 8 MB against a non-reading peer at 1.13 s, post-close refusal,
concurrent emit+close clean. `_batch`, `_chunk`, `_retry` unchanged and sound. Vendor limits in
code match SQS/SNS/Kinesis/Firehose/Datadog/New Relic/Honeycomb.

## 7. Not covered

No optional extra was installed (it breaks `mypy` for the checkout), so every third-party sink was
exercised through `sys.modules` fakes or read; real driver exception types and boto3's own retry
interaction with R2 were not run, and `tests/integration/` was not started. Free-threaded builds and
Windows. The `tests/` tree against the docstring rule. `docs/architecture.md` prose beyond the
anchors the README links to. PyPI rendering beyond `twine check`, and whether the Trusted Publisher
registration matches `release.yml`. Each auditor was capped; what fell below its cut line is not here.

## 8. Suggested grouping (a recommendation, not a decision)

- **Sink delivery correctness** — R1, R2, R5, R9, R14 (loss/duplication) as one spec; R7, R8, R10
  (construction-time validation and driver bounds) as a second beside it. R1 and R2 are the two I
  would not tag without.
- **Lifecycle residue** — R3, R4, R6, R12 in one small spec; R11 is a decision to reconsider or
  re-affirm in the register.
- **API freeze tidy** — P1..P5, P8 in one spec; it is the last chance for P1 and P2.
- **1.0 release surface** — D1..D5 and D8..D10, C2..C5, C7 need no spec: docs, metadata and
  scaffolding removal, but each on its own PR per the process. D6/D7 are a CI/settings PR.
- **The docstring cap (C1)** is a rule decision for the register before it is a sweep.
